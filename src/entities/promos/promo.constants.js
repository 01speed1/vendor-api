//const BUY_GOT = 'Buy One Got One';

const DISCOUNT_RATE = 'discountRate';
const DISCOUNT_PRICE = 'discountPrice';
const MULTI_BUY = 'multiBuy';
const FREE_PRODUCT = 'freeProduct';

const KEYS = [MULTI_BUY, DISCOUNT_RATE, DISCOUNT_PRICE, FREE_PRODUCT];

const TYPES = {
  MULTI_BUY,
  DISCOUNT_RATE,
  DISCOUNT_PRICE,
  FREE_PRODUCT
};

module.exports = { TYPES, KEYS };
