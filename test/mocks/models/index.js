const offerMock = require('./offer.mock');
const accountMock = require('./account.mock');
const orderMock = require('./order.mock');
const consumerMock = require('./consumer.mock');
const productOfferedMock = require('./productOffered.mock');
const serviceOfferedMock = require('./serviceOffered.mock');

module.exports = {
  offerMock,
  accountMock,
  orderMock,
  consumerMock,
  productOfferedMock,
  serviceOfferedMock
};
